{
  "name": "dot-maze-web",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc --noEmit && node build.js",
    "dev": "esbuild src/Game.ts --bundle --outfile=dist/bundle.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jsqr": "^1.4.0",
    "qrcode-generator": "^2.0.4",
    "trystero": "^0.25.4"
  }
}
